import React from "react";

const Hero = () => {
    return (
        <div className="hero-container">
            <h2>Take pride in your comfort</h2>
            <input 
                className="search"
                type="search"
                placeholder="Enter an address, neighborhood, city or ZIP code"

            />
        </div>
    )
}
export default Hero;